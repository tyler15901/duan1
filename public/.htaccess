Options -MultiViews
RewriteEngine On

# Chặn truy cập trực tiếp vào thư mục (bảo mật)
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f

# Chuyển tất cả request về index.php
RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]